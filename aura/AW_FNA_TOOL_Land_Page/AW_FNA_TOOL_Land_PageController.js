({
	onPageReferenceChange: function(component, event, helper)
	{
		/**
		 * Description : this will call do init of the component which will
		 * calling helper method to get the account id linked to the fnaId
		 **/
		helper.onPageReferenceChange(component, event, helper);
	}
});